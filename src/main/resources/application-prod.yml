server:
  port: 8088
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/jredmine
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate # 只校验数据库表结构是否匹配实体类，不做变更
    show-sql: false