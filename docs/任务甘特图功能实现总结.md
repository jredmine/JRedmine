# 任务甘特图功能实现总结

## 实现概述

本次实现了完整的任务甘特图数据获取接口，为项目管理系统提供了强大的任务可视化基础。该功能支持查询项目的所有任务及其时间线信息、依赖关系、完成进度等，方便前端展示甘特图。

## 新增文件

### 1. DTO类（数据传输对象）

#### 请求DTO
- **`IssueGanttRequestDTO.java`**: 甘特图查询请求参数
  - 支持按项目ID查询（必填）
  - 支持包含子项目任务
  - 支持按版本、跟踪器、状态、指派人筛选
  - 支持按时间范围筛选
  - 支持只显示有日期的任务

#### 响应DTO
- **`IssueGanttResponseDTO.java`**: 甘特图响应数据
  - 包含任务列表
  - 包含甘特图起止日期
  - 包含任务总数

- **`IssueGanttItemResponseDTO.java`**: 甘特图任务项
  - 包含任务基本信息（ID、标题、项目、跟踪器、状态等）
  - 包含时间信息（开始日期、截止日期）
  - 包含进度信息（完成度、预估工时）
  - 包含关联信息（父任务、版本、指派人等）
  - 包含依赖关系列表
  - 包含是否为里程碑标识

- **`IssueGanttDependencyDTO.java`**: 任务依赖关系
  - 依赖的任务ID
  - 依赖类型（precedes、follows、blocks、blocked）
  - 延迟天数

### 2. 文档
- **`任务甘特图接口使用指南.md`**: 完整的接口使用文档
  - 接口说明
  - 请求参数详解
  - 响应结构说明
  - 使用示例
  - 权限说明
  - 前端集成建议

## 修改的文件

### 1. IssueController.java
- 添加 `getIssueGantt` 接口方法
- 路径：`GET /api/issues/gantt`
- 权限：需要 `view_issues` 权限或系统管理员
- 添加了完整的接口文档注解

### 2. IssueService.java
- 添加 `getIssueGantt` 服务方法：处理甘特图数据查询
- 添加 `getSubProjectIds` 辅助方法：递归查询子项目
- 添加 `toIssueGanttItemDTO` 转换方法：转换任务为甘特图项
- 导入新增的DTO类

## 功能特性

### 1. 核心功能
- ✅ 按项目查询任务甘特图数据
- ✅ 支持包含子项目的任务
- ✅ 支持多种筛选条件（版本、跟踪器、状态、指派人、时间范围）
- ✅ 支持只显示有日期的任务
- ✅ 返回任务的依赖关系（precedes、follows、blocks、blocked）
- ✅ 自动计算甘特图的时间范围
- ✅ 标识里程碑（顶级任务）

### 2. 权限控制
- ✅ 需要认证（Bearer Token）
- ✅ 需要 `view_issues` 权限或系统管理员
- ✅ 私有任务仅项目成员可见
- ✅ 子项目权限验证

### 3. 数据完整性
- ✅ 包含任务基本信息（标题、描述、状态等）
- ✅ 包含关联信息（项目名、跟踪器名、状态名、指派人名等）
- ✅ 包含时间信息（开始日期、截止日期）
- ✅ 包含进度信息（完成度、预估工时）
- ✅ 包含依赖关系（类型、延迟天数）
- ✅ 包含树形结构信息（父任务ID）

### 4. 性能优化
- ✅ 使用MyBatis-Plus的LambdaQueryWrapper构建高效查询
- ✅ 批量查询依赖关系，避免N+1问题
- ✅ 支持筛选条件减少数据量
- ✅ 按树形结构排序（rootId, lft）

## 接口规范

### 请求示例
```bash
GET /api/issues/gantt?projectId=1&includeSubprojects=true&versionId=1
Authorization: Bearer {token}
```

### 响应示例
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "tasks": [
      {
        "id": 1,
        "subject": "实现用户管理模块",
        "projectId": 1,
        "projectName": "JRedmine",
        "trackerId": 2,
        "trackerName": "Feature",
        "statusId": 2,
        "statusName": "进行中",
        "statusClosed": false,
        "priorityId": 2,
        "priorityName": "普通",
        "assignedToId": 1,
        "assignedToName": "张三",
        "parentId": null,
        "startDate": "2026-01-01",
        "dueDate": "2026-01-31",
        "doneRatio": 30,
        "estimatedHours": 40.0,
        "fixedVersionId": 1,
        "fixedVersionName": "v1.0",
        "isMilestone": true,
        "dependencies": []
      }
    ],
    "ganttStartDate": "2026-01-01",
    "ganttEndDate": "2026-12-31",
    "totalCount": 10
  }
}
```

## 技术实现要点

### 1. 依赖关系处理
```java
// 批量查询依赖关系
List<IssueRelation> relations = issueRelationMapper.selectList(
    new LambdaQueryWrapper<IssueRelation>()
        .in(IssueRelation::getIssueFromId, issueIds)
        .in(IssueRelation::getRelationType, List.of("precedes", "follows", "blocks", "blocked")));

// 按源任务分组
Map<Long, List<IssueRelation>> issueRelationsMap = relations.stream()
    .collect(Collectors.groupingBy(r -> Long.valueOf(r.getIssueFromId())));
```

### 2. 子项目查询
```java
// 通过树形结构查询所有子项目
List<Project> subProjects = projectMapper.selectList(
    new LambdaQueryWrapper<Project>()
        .gt(Project::getLft, parentProject.getLft())
        .lt(Project::getRgt, parentProject.getRgt()));
```

### 3. 时间范围计算
```java
// 遍历所有任务，计算最早开始日期和最晚截止日期
for (Issue issue : issues) {
    if (issue.getStartDate() != null) {
        if (minStartDate == null || issue.getStartDate().isBefore(minStartDate)) {
            minStartDate = issue.getStartDate();
        }
    }
    if (issue.getDueDate() != null) {
        if (maxDueDate == null || issue.getDueDate().isAfter(maxDueDate)) {
            maxDueDate = issue.getDueDate();
        }
    }
}
```

### 4. ID字段筛选条件处理
```java
// 只有不为null且不为0时才添加筛选条件
// 这样可以正确处理前端传入0值的情况
if (requestDTO.getVersionId() != null && requestDTO.getVersionId() > 0) {
    queryWrapper.eq(Issue::getFixedVersionId, requestDTO.getVersionId());
}
if (requestDTO.getTrackerId() != null && requestDTO.getTrackerId() > 0) {
    queryWrapper.eq(Issue::getTrackerId, requestDTO.getTrackerId());
}
if (requestDTO.getStatusId() != null && requestDTO.getStatusId() > 0) {
    queryWrapper.eq(Issue::getStatusId, requestDTO.getStatusId());
}
if (requestDTO.getAssignedToId() != null && requestDTO.getAssignedToId() > 0) {
    queryWrapper.eq(Issue::getAssignedToId, requestDTO.getAssignedToId());
}
```

### 5. 私有任务可见性
```java
// 获取当前用户在相关项目中的成员身份
List<Member> members = memberMapper.selectList(
    new LambdaQueryWrapper<Member>()
        .eq(Member::getUserId, currentUser.getId())
        .in(Member::getProjectId, projectIds));

// 非项目成员只能看非私有任务
if (members.isEmpty()) {
    queryWrapper.eq(Issue::getIsPrivate, false);
}
```

## 前端集成建议

### 1. 推荐的甘特图库
- **DHTMLX Gantt**: 企业级，功能全面
- **Frappe Gantt**: 轻量级，易于使用
- **FullCalendar**: 灵活，支持多种视图

### 2. 数据展示
- 使用不同颜色区分任务状态（进行中/已完成/已关闭）
- 使用进度条显示完成度
- 使用连线显示依赖关系
- 使用特殊标记显示里程碑

### 3. 交互功能
- 点击任务查看详情
- 拖拽调整任务时间
- 缩放时间轴
- 筛选和搜索任务

## 测试建议

### 1. 单元测试
- 测试不同筛选条件的查询
- 测试子项目包含逻辑
- 测试依赖关系查询
- 测试权限验证

### 2. 集成测试
- 测试完整的请求-响应流程
- 测试大数据量场景
- 测试并发访问

### 3. 性能测试
- 测试不同任务数量下的响应时间
- 测试查询优化效果

## 后续优化建议

### 1. 功能增强
- [ ] 支持分页查询（大项目任务数量可能很多）
- [ ] 支持自定义排序
- [ ] 支持导出甘特图为图片或PDF
- [ ] 支持甘特图视图配置（周视图、月视图等）

### 2. 性能优化
- [ ] 添加缓存机制（Redis）
- [ ] 优化数据库查询（添加索引）
- [ ] 支持增量更新（WebSocket）

### 3. 用户体验
- [ ] 支持拖拽调整任务时间
- [ ] 支持拖拽创建依赖关系
- [ ] 支持批量操作任务
- [ ] 支持自动排期（基于依赖关系）

## 相关文档

- [任务甘特图接口使用指南](./任务甘特图接口使用指南.md)
- [任务管理接口清单](./任务管理接口清单.md)
- [工作流管理API使用指南](./工作流管理API使用指南.md)

## 版本信息

- **实现日期**: 2026-01-11
- **实现版本**: v1.0
- **实现人**: AI Assistant
